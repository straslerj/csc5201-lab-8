# service.yaml
apiVersion: v1
kind: Service
metadata:
  name: csc5201-lab-8
spec:
  ports:
    - port: 5000
      protocol: TCP
      targetPort: 5000
  selector:
    app: csc5201-lab-8
  type: LoadBalancer
